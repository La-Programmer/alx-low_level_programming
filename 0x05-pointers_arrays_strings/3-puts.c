#include "main.h"
#include <stdio.h>
/**
 * _puts - this function reutrns a character followed
 * by a trailing newline character.
 * @str: the argument is any random string.
 *
 * Return: my function returns nothing.
 */
void _puts(char *str)
{
	puts(str);
}
