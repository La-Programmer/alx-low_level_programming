#include "main.h"
#include <string.h>
/**
 * _strlen - this function gets the length of a string.
 * @s: the parameter is pointer to a string.
 *
 * Return: my function returns an integer which is the length
 * of the string the the pointer *s points to.
 */
int _strlen(char *s)
{
	return(strlen(s));
}
