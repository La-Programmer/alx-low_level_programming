#include "main.h"
#include <stdlib.h>
/**
 *  _abs - my function computes the absolute
 * value of an integer
 * @a: the argument is an integer
 *
 * Return: my function returns the absolute value
 * of the integer
 */
int _abs(int a)
{

	int b = abs(a);

	return (b);
}
